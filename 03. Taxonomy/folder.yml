info:
  name: 03. Taxonomy
  type: folder
  seq: 4

request:
  auth: inherit

docs:
  content: |-
    # Taxonomy

    HSIS taxonomy tree lookups. **Tenant-scoped** (`x-tenant-id` + `accept-language`).

    - **Search** returns a slimmer `{ id, name, code }` shape — send
      `x-api-version: 2`.
    - **Get Terms** resolves exact taxonomy codes.
  type: text/markdown
