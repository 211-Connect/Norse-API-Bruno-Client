info:
  name: "1. Search Taxonomies"
  type: http
  seq: 1

http:
  method: get
  url: "{{baseUrl}}/taxonomy?query=housing&page=1"
  headers:
    - name: X-API-Key
      value: "{{SERVICES_APIKEY}}"
    - name: x-api-version
      value: "2"
    - name: x-tenant-id
      value: "{{tenantId}}"
    - name: accept-language
      value: "{{acceptLanguage}}"
  params:
    - name: query
      value: housing
      type: query
    - name: page
      value: "1"
      type: query
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  # Search Taxonomies

  `GET /taxonomy` with `x-api-version: 2` — prefix search by name/code. Returns a
  simplified `{ id, name, code }` list. `query` (≤500), `page` (≥1).
